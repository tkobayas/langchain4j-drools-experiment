package org.example;

import org.example.langchain4j.drools.domain.LoanApplication;

rule AdultApplication
when
    $l: LoanApplication( amount <= 5000, applicant.age >= 18 )
then
    System.out.println("AdultApplication fired");
    modify($l) { setApproved(true) };
end
